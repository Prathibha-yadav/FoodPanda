<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
  <title>Recipe Generator</title>
  <link href="/styles.css" rel="stylesheet">
</head>
<body>
    <%- include('header.ejs') %>
    <div class="max-w-screen w-full bg-white-400 p-6 rounded-lg shadow-lg">
        <h1 class="text-3xl font-bold mb-6 text-center text-blue-500">Recipe Generator</h1>
        <form action="/GeminiResponse" method="post" class="mb-8">
          <label for="textInput" class="block text-sm font-medium text-gray-700 mb-2">Enter available ingredients:</label>
          <input type="text" id="textInput" name="text" required class="mt-1 block w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-blue-500 focus:border-blue-500 sm:text-sm">
          <button type="submit" class="mt-4 w-full inline-flex items-center justify-center px-4 py-2 border border-transparent rounded-md shadow-sm text-white bg-blue-500 hover:bg-blue-600 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500">
            Generate Recipe
          </button>
        </form>
        <% if (response !== null) { %>
          <div class="border-t mt-8 pt-8">
            <h2 class="text-xl font-bold mb-4 text-blue-500">Generated Recipe:</h2>
            <pre class="whitespace-pre-wrap"><%= response %></pre>
          </div>
        <% } %>
      </div>
      
  <%- include('footer.ejs') %>
</body>
</html>
